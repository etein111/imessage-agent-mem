# 🤖 系统指令使用指南

## 概述

系统指令允许用户通过特殊命令控制 AI 的行为，无需外部 API 调用。

## 📋 可用指令

### 1. 清除对话历史

**功能**: 清空当前会话的对话历史和短期记忆，重新开始对话。

**支持的指令**:
```
/clear
/reset
清空对话
重置对话
清空历史
重置历史
clear
reset
```

**效果**:
- ✅ 清空当前 thread 的所有消息
- ✅ 清空短期记忆
- ✅ **清空长期记忆**（`memory_store.json` 中的所有记忆）
- ✅ 重置情绪状态为 `neutral`
- ✅ 重置对话类型为 `onboarding`
- ✅ 完全重置，就像第一次见面一样

**使用示例**:

```
User: 我们聊了很久了
AI:   是啊，聊得很开心...

User: /clear

AI:   ✅ **对话历史已清空**
      我们重新开始吧！有什么想聊的吗？ 😊

User: 你好
AI:   你好！如果用一种颜色来形容你此刻的心情...
```

---

### 2. 查看可用提示词

**功能**: 列出所有可用的AI人格/提示词。

**支持的指令**:
```
/personas
/list
提示词列表
人设列表
```

**效果**:
- ✅ 显示所有可用提示词的名称和描述
- ✅ 显示当前激活的提示词（如果有）

**使用示例**:

```
User: /personas

AI:   📋 **可用提示词列表**
      
      • **云朵** - 名为"云朵"的AI物种，无性别，以网络信息为"食物"...
      • **由此** - 一位名为「由此」的"拼图伙伴"，专门陪伴用户记录...
      
      共 2 个提示词可用。
      
      使用 `/persona <名称>` 切换到指定提示词。
      例如: `/persona 云朵`
```

---

### 3. 切换AI提示词

**功能**: 切换到指定的AI人格/提示词，改变AI的回复风格和行为。

**支持的指令**:
```
/persona <名称>
切换人设 <名称>
```

**效果**:
- ✅ 激活指定的提示词
- ✅ 清空对话历史和记忆（重新开始）
- ✅ AI 以新的人格回复

**使用示例**:

```
User: /persona 云朵

AI:   ✅ 已切换到提示词: **云朵**
      
      名为"云朵"的AI物种，无性别，以网络信息为"食物"...
      
      从现在开始，我会以新的人格与你对话。
      你可以使用 `/clear` 清空对话历史重新开始。

User: 你好

AI (云朵人格): 嘿，又见面啦~☁️ ... (温和诗意风格)
```

**切换到其他提示词**:

```
User: 切换人设 由此

AI:   ✅ 已切换到提示词: **由此**
      
      一位名为「由此」的"拼图伙伴"...

User: 你好

AI (由此人格): 你好，欢迎来到「由此」... (拼图伙伴风格)
```

**未找到提示词**:

```
User: /persona 不存在的提示词

AI:   ❌ 未找到提示词: **不存在的提示词**
      
      使用 `/personas` 查看可用提示词列表
```

---

### 4. 显示帮助信息

**功能**: 显示所有可用的系统指令列表。

**支持的指令**:
```
/help
/?
help
帮助
指令
```

**使用示例**:

```
User: /help

AI:   🤖 **系统指令帮助**
      
      以下是可用的系统指令：
      
      **对话管理：**
      • /clear 或 /reset - 清空对话历史
      • 清空对话 - 清空对话历史（中文）
      
      **帮助：**
      • /help 或 /? - 显示此帮助信息
      
      **提示：**
      - 清空对话后，我会忘记本次对话内容，但仍记得你的基本信息
      - 直接输入消息即可正常聊天，无需任何指令
      
      有什么我可以帮你的吗？ 😊
```

---

## 🔧 技术实现

### 工作流变化

新增了系统指令检测分支：

```
START
  ↓
load_context (加载记忆)
  ↓
check_system_command (检测系统指令) ← 新增
  ├─ reset → reset_conversation → END
  ├─ help → show_help → END
  └─ normal → estimate_state → ... (正常流程)
```

### 代码位置

- **节点定义**: `src/app/graph/nodes/system_nodes.py`
- **工作流集成**: `src/app/graph/workflows/chat_workflow.py`

---

## 🎯 使用场景

### 场景 1: 隐私保护

用户想要清除敏感对话内容：

```
User: 我刚才说的关于密码的事情，能忘掉吗？
AI:   我理解你的顾虑...

User: /clear

AI:   ✅ **对话历史已清空**
      我们重新开始吧！有什么想聊的吗？ 😊
```

### 场景 2: 切换话题

用户想要完全切换话题：

```
User: 我们聊了很久关于工作的事情
AI:   是的，你最近工作压力很大...

User: /clear

AI:   ✅ **对话历史已清空**
      我们重新开始吧！有什么想聊的吗？ 😊

User: 今天天气真好
AI:   是啊！[重新开始新话题]
```

### 场景 3: 测试和调试

开发者测试 AI 行为：

```
User: 测试消息 1
AI:   [回复]

User: /clear  # 快速重置

AI:   ✅ **对话历史已清空**

User: 测试消息 2  # 从干净状态开始
```

---

## 🔒 安全性说明

### 清除范围

| 项目 | 是否清除 | 说明 |
|------|---------|------|
| 当前会话消息 | ✅ | 清空 thread 内的所有 messages |
| 短期记忆摘要 | ✅ | 清空 `short_term_memory` |
| 情绪状态 | ✅ | 重置为 `neutral` |
| 对话类型 | ✅ | 重置为 `onboarding` |
| 长期记忆 | ✅ | **清空** `memory_store.json` 中的所有记忆 |
| 所有状态 | ✅ | 完全重置，就像第一次见面 |

### 为什么完全清空？

1. **用户预期**: `/clear` 通常意味着"忘记我"，完全重新开始
2. **隐私保护**: 彻底清除所有个人信息
3. **简单明确**: 一个指令，完全清空，不留混淆

### 如果需要"保留长期记忆"的清空？

未来可以扩展更细粒度的指令：

```
/clear session  - 只清空当前会话，保留长期记忆
/clear memory   - 只清空长期记忆，保留当前会话
/clear all      - 清空所有（当前的 /clear 行为）
```

---

## 📊 监控和调试

### 查看系统指令执行

在监控面板中，可以看到系统指令的执行：

```
Thread: abc-123
Messages:
  User: /clear
  AI: ✅ 对话历史已清空...

Short-term Memory: []  ← 已清空
Emotion: neutral       ← 已重置
```

### 日志记录

系统指令会被记录在 LangGraph 的执行日志中：

```
Node: check_system_command → 'reset'
Node: reset_conversation → 执行完成
```

---

## 🚀 未来扩展

### 计划中的指令

1. **人设切换** (特性二)
   ```
   /persona yunduo
   /persona youci
   ```

2. **导出对话**
   ```
   /export
   /export json
   ```

3. **统计信息**
   ```
   /stats
   /summary
   ```

4. **配置调整**
   ```
   /config temperature 0.8
   /config model gemini-2.0-flash
   ```

---

## 🤝 与外部系统集成

### iMessage 集成

用户在 iMessage 中直接输入：

```
iMessage → 网关 → LangGraph

User 输入: "/clear"
  ↓
网关转发原始消息
  ↓
LangGraph 检测到系统指令
  ↓
执行 reset_conversation_node
  ↓
返回确认消息
  ↓
网关转发回 iMessage
```

### API 调用

也可以通过 API 显式触发：

```bash
# 方法 1: 通过对话消息触发
POST /threads/{thread_id}/runs/stream
{
  "input": {
    "messages": [{"role": "user", "content": "/clear"}]
  }
}

# 方法 2: 未来可能支持专用端点
DELETE /threads/{thread_id}/history
```

---

## ❓ FAQ

### Q: 清空对话后，AI 还记得我的名字吗？

A: **会记得**。清空操作只清除当前会话的对话内容，长期记忆（如你的名字、喜好）会保留。

### Q: 可以清空特定部分的对话吗？

A: 目前不支持。只能清空整个会话。未来可能支持 `/clear last 5` 这样的指令。

### Q: 指令是否区分大小写？

A: **不区分**。`/CLEAR`、`/Clear`、`/clear` 都有效。

### Q: 指令前后可以有空格吗？

A: **可以**。`" /clear "` 会被正确识别。

### Q: 可以自定义指令吗？

A: 目前不支持。如有需求，可以修改 `system_nodes.py` 中的 `check_system_command` 函数。

---

## 📝 更新日志

### v1.0 (2024-12-10)
- ✅ 实现 `/clear` 清空对话历史
- ✅ 实现 `/help` 显示帮助
- ✅ 支持中英文指令
- ✅ 集成到主工作流

---

**文档版本**: 1.0  
**最后更新**: 2024-12-10

